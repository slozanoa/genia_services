# Credit Scoring

## 🧠 Comandos útiles de pyenv

### 📌 Listar versiones de Python disponibles
```bash
pyenv install --list
```

### 📌 Instalar una versión específica

Por ejemplo Python 3.11.5:

```bash
pyenv install 3.11.5
```

### 📌 Ver versiones instaladas
```bash
pyenv versions
```

### 📌 Usar una versión global (por defecto)
```bash
pyenv global 3.11.5
```

### 📌 Usar una versión local para un proyecto (en la carpeta)

Dentro de tu proyecto:

```bash
pyenv local 3.11.5
```
